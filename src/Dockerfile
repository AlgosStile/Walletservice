# Говорим сккчать такой то образ: maven/Java 8 образ
FROM maven:3.6.0-jdk-8

# Установливает рабочую директорию в /app
WORKDIR /app

# Копирует текущий код в /app
ADD . /app

# Запускает Maven для сборки и запуска тестов
CMD ["mvn", "test"]